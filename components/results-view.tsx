"use client"

import { useState } from "react"
import { Button } from "@/components/ui/button"
import { BellCurve } from "@/components/bell-curve"
import type { ScoreResult } from "@/lib/matrix"
import {
  BookOpen,
  ChevronDown,
  ChevronUp,
  Download,
  RotateCcw,
  Sparkles,
  Target,
  TrendingUp,
} from "lucide-react"

function classify(iq: number) {
  if (iq >= 130) return "Very Superior"
  if (iq >= 120) return "Superior"
  if (iq >= 110) return "High Average"
  if (iq >= 90) return "Average"
  if (iq >= 80) return "Low Average"
  return "Below Average"
}

export function ResultsView({
  result,
  onRestart,
}: {
  result: ScoreResult
  onRestart: () => void
}) {
  const { iq, percentile, correct, total, theta, sem, subscales } = result
  const [showMethodology, setShowMethodology] = useState(false)

  const downloadPdf = async () => {
    const { jsPDF } = await import("jspdf")
    const doc = new jsPDF({ unit: "pt", format: "a4" })
    const pageW = doc.internal.pageSize.getWidth()
    const date = new Date().toLocaleDateString(undefined, {
      year: "numeric",
      month: "long",
      day: "numeric",
    })

    // Header Band
    doc.setFillColor(15, 15, 15)
    doc.rect(0, 0, pageW, 110, "F")
    
    doc.setTextColor(255, 255, 255)
    doc.setFont("helvetica", "bold")
    doc.setFontSize(22)
    doc.text("OpenCognitive", 40, 52)
    
    doc.setTextColor(160, 160, 160)
    doc.setFont("helvetica", "normal")
    doc.setFontSize(9)
    doc.text("CLINICAL COGNITIVE DOSSIER // INDEPENDENT PSYCHOMETRIC INSTRUMENT", 40, 70)
    doc.text(`COMPLETED ON: ${date.toUpperCase()}`, 40, 84)

    // Body
    doc.setTextColor(30, 30, 30)
    doc.setFont("helvetica", "bold")
    doc.setFontSize(14)
    doc.text("I. PSYCHOMETRIC SCORES", 40, 150)

    doc.setFont("helvetica", "bold")
    doc.setFontSize(11)
    doc.text(`ESTIMATED IQ SCORE: ${iq}`, 40, 180)
    doc.text(`PERCENTILE RANK: ${percentile}th Percentile`, 40, 196)
    doc.text(`LATENT ABILITY (theta): ${theta.toFixed(2)}`, 40, 212)
    doc.text(`MEASUREMENT STANDARD ERROR (SEM): ${sem.toFixed(2)}`, 40, 228)
    
    doc.setFont("helvetica", "normal")
    doc.text(`CLASSIFICATION: ${classify(iq).toUpperCase()}`, 40, 250)
    doc.text(`RAW ACCURACY: ${correct} OF ${total} ITEMS CORRECT (${Math.round((correct / total) * 100)}%)`, 40, 266)

    // Subscales section
    doc.setFont("helvetica", "bold")
    doc.text("II. COGNITIVE DOMAIN BREAKDOWN", 40, 310)
    doc.setFont("helvetica", "normal")
    doc.text(`- FLUID INTELLIGENCE (Gf): ${subscales.fluid}%`, 50, 330)
    doc.text(`- VISUOSPATIAL PROCESSING (Gv): ${subscales.spatial}%`, 50, 345)
    doc.text(`- CRYSTALLIZED REASONING (Gc): ${subscales.verbal}%`, 50, 360)

    // Methodology & References
    doc.setFillColor(248, 246, 242)
    doc.rect(40, 400, pageW - 80, 180, "F")
    
    doc.setFont("helvetica", "bold")
    doc.setFontSize(9)
    doc.setTextColor(40, 40, 40)
    doc.text("III. DESIGN & STANDARDIZATION DETAILS", 50, 420)
    
    doc.setFont("helvetica", "normal")
    doc.setFontSize(8)
    doc.setTextColor(80, 80, 80)
    const methodDesc = 
      "Scoring uses the 2PL/3PL Item Response Theory (IRT) model, estimated via Expected A Posteriori (EAP) " +
      "Bayesian scoring integration with standard normal population prior quadrature nodes. Estimated IQ is mapped " +
      "onto a standard normal population distribution with a mean of 100 and standard deviation of 15. Frameworks " +
      "include the International Cognitive Ability Resource (ICAR) and Sandia Matrices."
    doc.text(doc.splitTextToSize(methodDesc, pageW - 110), 50, 438)

    // Sources list
    doc.setFont("helvetica", "bold")
    doc.text("PRIMARY REFERENCES:", 50, 505)
    doc.setFont("helvetica", "normal")
    doc.text("1. Condon, D. M., & Revelle, W. (2014). The International Cognitive Ability Resource.", 55, 520)
    doc.text("2. Matzen, L. E., et al. (2010). Measuring Intelligence with the Sandia Matrices.", 55, 532)
    doc.text("3. Chierchia, G., et al. (2019). The Matrix Reasoning Item Bank (MaRs-IB).", 55, 544)

    // Footer Disclaimer
    doc.setFontSize(7.5)
    doc.setTextColor(120, 120, 120)
    const disclaimer =
      "DISCLAIMER: This assessment is a student research project. The scores generated by this instrument are for " +
      "educational and simulation purposes only, and can never be substituted for a professional clinical assessment or " +
      "diagnostic evaluation by a licensed professional."
    doc.text(doc.splitTextToSize(disclaimer, pageW - 80), 40, 615)

    doc.save(`opencognitive-dossier-${iq}.pdf`)
  }

  return (
    <main className="min-h-screen bg-background text-foreground flex flex-col justify-between selection:bg-primary/20">
      
      {/* Top Header */}
      <header className="max-w-4xl w-full mx-auto px-6 py-6 flex items-center justify-between border-b border-border">
        <div className="flex items-center gap-2">
          <div className="h-6 w-6 rounded-full bg-primary flex items-center justify-center text-primary-foreground font-bold">
            <Sparkles className="h-3.5 w-3.5" />
          </div>
          <span className="font-semibold text-base tracking-tight text-white font-sans">
            OpenCognitive
          </span>
        </div>
        <span className="text-xs font-semibold text-muted-foreground uppercase tracking-wider">
          Assessment Complete
        </span>
      </header>

      {/* Main Results Panel */}
      <section className="flex-1 max-w-3xl w-full mx-auto px-6 py-12 space-y-8">
        
        {/* Student Research Disclaimer Card */}
        <div className="bg-amber-500/5 border border-amber-500/20 px-6 py-4 rounded-2xl text-xs text-amber-200/80 leading-relaxed text-center font-sans">
          <span className="font-bold text-amber-400 uppercase tracking-wider block mb-1">Student Research Project Disclaimer</span>
          This tool is a student project developed for educational research in cognitive psychometrics. 
          The estimated scores generated by this simulator <strong>can never be substituted for a professional clinical assessment or diagnostic evaluation</strong> by a licensed psychologist.
        </div>

        {/* Editorial Scoring Block */}
        <div className="p-8 bg-card rounded-2xl border border-border w-full flex flex-col items-center justify-center text-center glow-card">
          <span className="text-xs font-bold uppercase tracking-widest text-muted-foreground mb-1">
            Estimated IQ Index
          </span>
          <div className="text-8xl sm:text-9xl font-black tracking-tighter text-white leading-none">
            {iq}
          </div>
          <span className="text-sm font-bold uppercase tracking-wider text-primary border border-primary/20 bg-primary/5 px-4 py-1.5 rounded-full mt-6">
            {classify(iq)}
          </span>
          <div className="mt-4 flex gap-6 text-xs text-muted-foreground font-medium">
            <span>Ability (θ): {theta.toFixed(2)}</span>
            <span>SEM: {sem.toFixed(2)}</span>
          </div>
        </div>

        {/* Population Percentile / Accuracy Grid */}
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <div className="p-5 bg-card rounded-2xl border border-border flex items-center gap-4 glow-card">
            <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary">
              <TrendingUp className="h-5 w-5" />
            </div>
            <div>
              <p className="text-2xl font-black text-white">{percentile}th</p>
              <p className="text-xs text-muted-foreground font-medium">Population Percentile Rank</p>
            </div>
          </div>

          <div className="p-5 bg-card rounded-2xl border border-border flex items-center gap-4 glow-card">
            <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary">
              <Target className="h-5 w-5" />
            </div>
            <div>
              <p className="text-2xl font-black text-white">
                {correct} <span className="text-sm text-muted-foreground font-medium">/ {total}</span>
              </p>
              <p className="text-xs text-muted-foreground font-medium">Items Answered Correctly</p>
            </div>
          </div>
        </div>

        {/* Cognitive Index Progress Bars */}
        <div className="p-6 bg-card rounded-2xl border border-border space-y-4 glow-card">
          <h2 className="text-xs font-bold uppercase tracking-wider text-muted-foreground border-b border-border pb-2.5">
            Cognitive Dimension Index
          </h2>
          <div className="space-y-4 font-sans">
            <div>
              <div className="flex justify-between text-sm font-semibold mb-1.5">
                <span className="text-white">Fluid Reasoning (Gf)</span>
                <span className="text-primary">{subscales.fluid}%</span>
              </div>
              <div className="h-2 bg-secondary rounded-full overflow-hidden">
                <div className="h-full bg-primary" style={{ width: `${subscales.fluid}%` }} />
              </div>
            </div>
            <div>
              <div className="flex justify-between text-sm font-semibold mb-1.5">
                <span className="text-white">Visuospatial Processing (Gv)</span>
                <span className="text-primary">{subscales.spatial}%</span>
              </div>
              <div className="h-2 bg-secondary rounded-full overflow-hidden">
                <div className="h-full bg-primary" style={{ width: `${subscales.spatial}%` }} />
              </div>
            </div>
            <div>
              <div className="flex justify-between text-sm font-semibold mb-1.5">
                <span className="text-white">Crystallized Logic (Gc)</span>
                <span className="text-primary">{subscales.verbal}%</span>
              </div>
              <div className="h-2 bg-secondary rounded-full overflow-hidden">
                <div className="h-full bg-primary" style={{ width: `${subscales.verbal}%` }} />
              </div>
            </div>
          </div>
        </div>

        {/* Bell Curve Calibration */}
        <div className="p-6 bg-card rounded-2xl border border-border glow-card">
          <div className="mb-4 flex items-center justify-between text-xs font-semibold text-muted-foreground">
            <h2 className="uppercase">Normal Distribution</h2>
            <span>Mean 100 / SD 15</span>
          </div>
          <BellCurve iq={iq} percentile={percentile} />
          <p className="mt-4 text-center text-sm text-muted-foreground leading-relaxed">
            Your performance places you at the <span className="font-bold text-white">{percentile}th percentile</span> of the general population.
          </p>
        </div>

        {/* Collapsible Methodology Accordion */}
        <div className="bg-card rounded-2xl border border-border overflow-hidden glow-card">
          <button
            onClick={() => setShowMethodology(!showMethodology)}
            className="flex w-full items-center justify-between px-6 py-4 hover:bg-secondary/40 transition-colors font-semibold text-sm"
          >
            <div className="flex items-center gap-2.5">
              <BookOpen className="h-4 w-4 text-primary" />
              <span className="text-white">Methodology & Academic Sources</span>
            </div>
            {showMethodology ? <ChevronUp className="h-4 w-4" /> : <ChevronDown className="h-4 w-4" />}
          </button>
          
          {showMethodology && (
            <div className="border-t border-border px-6 py-5 space-y-4 text-sm text-muted-foreground leading-relaxed font-sans">
              <div>
                <h3 className="font-bold text-white mb-1">Item Response Theory (IRT) & EAP</h3>
                <p>
                  Scoring utilizes <strong>Bayesian Expected A Posteriori (EAP)</strong> ability estimation over 41 Gauss-Hermite quadrature nodes. 
                  This calculates the probability of ability ($\theta$) given your specific pattern of responses, incorporating a 3PL logistic model (item difficulty, discrimination, and guessing floor).
                </p>
              </div>

              <div>
                <h3 className="font-bold text-white mb-1">Academic Standards</h3>
                <ul className="list-disc pl-5 space-y-1">
                  <li>
                    <strong>ICAR (International Cognitive Ability Resource):</strong> Public-domain validated items for unproctored online testing.
                  </li>
                  <li>
                    <strong>Sandia Matrices:</strong> Standardized procedural layout logic, featuring Object Relations and logic disjunctions (AND, OR, XOR).
                  </li>
                  <li>
                    <strong>MaRs-IB:</strong> Nonverbal reasoning matrices calibrated for adult and adolescent cohorts.
                  </li>
                </ul>
              </div>

              <div>
                <h3 className="font-bold text-white mb-1 text-xs">Primary References</h3>
                <ol className="list-decimal pl-5 space-y-1 mt-1 text-xs">
                  <li>Condon, D. M., & Revelle, W. (2014). The International Cognitive Ability Resource: Development and initial validation. *Journal of Intelligence*.</li>
                  <li>Matzen, L. E., et al. (2010). Measuring Intelligence with the Sandia Matrices: Psychometric Review and Recommendations. *Sandia National Laboratories Report*.</li>
                </ol>
              </div>
            </div>
          )}
        </div>

        {/* Action Controls */}
        <div className="flex flex-col sm:flex-row items-center justify-center gap-4 pt-6">
          <Button
            size="lg"
            onClick={downloadPdf}
            className="w-full sm:w-auto h-14 rounded-full px-8 text-base font-semibold bg-white hover:bg-neutral-200 text-black transition-all cursor-pointer shadow-md"
          >
            <Download className="mr-2 h-4 w-4" />
            Download PDF Report
          </Button>
          <Button
            size="lg"
            variant="secondary"
            onClick={onRestart}
            className="w-full sm:w-auto h-14 rounded-full px-8 text-base font-semibold border border-border bg-transparent hover:bg-secondary/40 text-foreground cursor-pointer"
          >
            <RotateCcw className="mr-2 h-4 w-4" />
            Retake Assessment
          </Button>
        </div>

      </section>

      {/* Bottom Technical Status Bar */}
      <footer className="max-w-4xl w-full mx-auto px-6 py-6 border-t border-border text-center text-xs text-muted-foreground space-y-2">
        <div>Developed by Ritesh. OpenCognitive diagnostic battery completed locally.</div>
        <div>
          <a 
            href="https://github.com/riteshrajas/OpenCognitive" 
            target="_blank" 
            rel="noopener noreferrer" 
            className="hover:text-white transition-colors underline underline-offset-4"
          >
            GitHub Repository (Fork & Read)
          </a>
        </div>
      </footer>

    </main>
  )
}
